// HyperlyPrompts (curated prompt library) has been removed for the
// personal/Windows-only build. It was a Hyperly Pro feature that pulled
// prompts from the Hyperly-hosted service. Users can still create and
// manage their own local system prompts via the same /system-prompts page.
const HyperlyPrompts = () => {
  return null;
};

export default HyperlyPrompts;
